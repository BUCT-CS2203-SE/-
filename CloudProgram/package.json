{
  "name": "cloud-program",
  "version": "1.0.0",
  "private": true,
  "description": "add shared dependencies for cloud functions",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:clouddb": "agc clouddb deploy",
    "deploy:cloudfunc": "agc function deploy",
    "deploy": "npm run deploy:clouddb && npm run deploy:cloudfunc",
    "build": "node scripts/automation.js build",
    "deploy:all": "node scripts/automation.js deploy",
    "clean": "node scripts/automation.js clean",
    "watch": "node scripts/automation.js watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@agconnect/api": "^1.3.2",
    "@agconnect/database": "^1.3.2",
    "@agconnect/database-server": "^1.0.7"
  },
  "devDependencies": {
    "@types/node": "^18.11.11",
    "typescript": "^5.4.2"
  },
  "publishConfig": {
    "registry": "https://repo.huaweicloud.com/repository/npm/"
  }
}