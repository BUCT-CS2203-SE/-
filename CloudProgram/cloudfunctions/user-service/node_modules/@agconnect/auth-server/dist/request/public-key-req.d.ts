import { RequestConstructor } from "@agconnect/common-server";
export declare class PublicKeyReq implements RequestConstructor {
    getUrl(): string;
    getHeader(): Promise<any>;
    getBody(): any;
}
