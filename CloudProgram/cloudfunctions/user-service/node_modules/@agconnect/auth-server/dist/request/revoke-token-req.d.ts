import { RequestConstructor, AGCClient } from "@agconnect/common-server";
export declare class RevokeTokenReq implements RequestConstructor {
    private readonly url;
    private client;
    private uid;
    constructor(uid: string, client: AGCClient);
    getUrl(useBackUrl?: boolean): string;
    getHeader(): Promise<any>;
    getBody(): any;
}
